import { Metadata } from 'next';

/**
 * Metadata for the Character Generator page.
 * This needs to be in a separate file from the client component.
 */
export const metadata: Metadata = {
  title: 'Character Generator | Studio',
  description: 'Create custom characters for your manga and comics',
};